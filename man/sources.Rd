% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sources.R
\name{sources}
\alias{sources}
\alias{source_csv}
\alias{source_redcap}
\alias{source_haven}
\alias{source_db}
\alias{source_dataframe}
\title{Describe a CSV / REDCap / statistical-file / database / data.frame source}
\usage{
source_csv(path, ...)

source_redcap(
  url = Sys.getenv("REDCAP_URI"),
  token = Sys.getenv("REDCAP_TOKEN"),
  fields = NULL,
  ...
)

source_haven(path, ...)

source_db(conn, query = NULL, table = NULL, ...)

source_dataframe(df)
}
\arguments{
\item{path}{Path to a \code{.csv} file (\code{source_csv}) or a \code{.sav}/\code{.dta}/\code{.sas7bdat}
file (\code{source_haven}).}

\item{...}{Passed to the underlying reader.}

\item{url, token}{REDCap API URI and token (default from the \code{REDCAP_URI} /
\code{REDCAP_TOKEN} environment variables, e.g. your \code{.Renviron}).}

\item{fields}{Optional character vector of REDCap fields to pull (default all).}

\item{conn}{A \code{DBI} connection (for \code{source_db}).}

\item{query}{A SQL query string (for \code{source_db}).}

\item{table}{A table name to read (for \code{source_db}).}

\item{df}{An in-memory data.frame / tibble.}
}
\value{
A \code{ch_source} object.
}
\description{
These constructors only \emph{describe} where the data lives; \code{\link[=read_source]{read_source()}}
actually reads it. This keeps the source definition portable (e.g. to pass
around a pipeline or run at another site).
}
\examples{
src <- source_csv("participants.csv")
\dontrun{ raw <- read_source(src) }
}
